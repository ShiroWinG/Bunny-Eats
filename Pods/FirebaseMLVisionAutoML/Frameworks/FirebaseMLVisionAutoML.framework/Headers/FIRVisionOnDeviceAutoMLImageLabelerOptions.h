#import <Foundation/Foundation.h>

NS_ASSUME_NONNULL_BEGIN

/** Options for an on-device image labeler using models generated by AutoML. */
NS_SWIFT_NAME(VisionOnDeviceAutoMLImageLabelerOptions)
@interface FIRVisionOnDeviceAutoMLImageLabelerOptions : NSObject

/**
 * The confidence threshold for labels returned by the image labeler. Labels returned by the image
 * labeler will have a confidence level higher or equal to the given threshold. Values must be in
 * range [0, 1]. If unset or an invalid value is set, the default threshold of 0.5 is used. There is
 * no limit on the maximum number of labels returned by an on-device image labeler.
 */
@property(nonatomic) float confidenceThreshold;

/**
 * The name of a model generated by AutoML that’s initially hosted on the server and will be
 * downloaded to the device.
 */
@property(nonatomic, copy, readonly, nullable) NSString *remoteModelName;

/** The name of a model generated by AutoML that is stored locally on the device. */
@property(nonatomic, copy, readonly, nullable) NSString *localModelName;

/**
 * Creates a new instance of `VisionOnDeviceAutoMLImageLabelerOptions` with the given remote and/or
 * local model name(s). At least one model name must be provided. If both remote and local model
 * names are provided, then the remote model takes priority.
 *
 * @param remoteModelName The name of a model generated by AutoML that’s initially hosted on the
 *     server and will be downloaded to the device. Pass `nil` if only the provided local model name
 *     should be used.
 * @param localModelName The name of a model generated by AutoML that is stored locally on the
 *     device. Pass `nil` if only the provided remote model name should be used.
 * @return `VisionOnDeviceAutoMLImageLabelerOptions` instance with the given remote and/or local
 *     model name(s).
 */
- (instancetype)initWithRemoteModelName:(nullable NSString *)remoteModelName
                         localModelName:(nullable NSString *)localModelName
    NS_DESIGNATED_INITIALIZER;

/** Unavailable. */
- (instancetype)init NS_UNAVAILABLE;

@end

NS_ASSUME_NONNULL_END
